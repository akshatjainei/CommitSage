{
  "inputs": [
    {
      "type": "promptString"
    }
  ],
  "servers": {
    "github": {
      "command": "npx",
      "args": [
        "-y", 
        "@modelcontextprotocol/server-github",
        "--github-token", "${GITHUB_TOKEN}"
      ],
      "env": {
        "GITHUB_TOKEN": "${GITHUB_TOKEN}"
      }
    }
  }
}
